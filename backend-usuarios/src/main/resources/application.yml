server:
  port: 8081

spring:
  datasource:
    url: jdbc:h2:mem:usuariosdb;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password: 
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

jwt:
  secret: cambia-esto-por-una-variable-de-entorno
  expirationMs: 3600000
